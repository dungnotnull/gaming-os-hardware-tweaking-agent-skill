# Enhanced requirements for gaming-os-hardware-tweaking v1.0.0
# Core dependencies
requests>=2.31.0
feedparser>=6.0.10
python-dateutil>=2.8.2

# Optional: advanced web crawling
crawl4ai>=0.3.0

# Development & testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-benchmark>=4.0.0
mypy>=1.5.0
ruff>=0.1.0
black>=23.0.0

# Platform-specific (Windows)
pywin32>=306; sys_platform == 'win32'
wmi>=1.5.1; sys_platform == 'win32'

# Platform-specific (Linux)  
distro>=1.8.0; sys_platform == 'linux'
